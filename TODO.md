# TODO: Add Personal Email ID and Official Email ID fields to AddEmployeePage2 form

- [x] Edit `src/data/addEmployeeFormSchema.json` to insert the two new email fields after `alternateContactNo`.
- [x] Edit `src/data/addEmployeeFormSchema.ts` to include `'personalEmailId'` and `'officialEmailId'` in the colSpan 4 logic.
